class Solution:
    def findFloor(self,arr,k):
        #Your code here
        a=0
        b=-1
        for i in range(len(arr)):
            if arr[i]<=k and arr[i]>a:
                a=arr[i]
                b=i
        return b
